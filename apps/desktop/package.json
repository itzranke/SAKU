{
  "name": "@saku/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "echo 'SAKU Desktop: turbo build step = typecheck-only stub. Full native bundling runs locally via `pnpm --filter @saku/desktop dev|exec tauri build` (requires Tauri system deps), NOT in CI turbo pipeline.'"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
